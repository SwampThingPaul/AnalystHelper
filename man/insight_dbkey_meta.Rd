% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DBHydro_Insight.R
\name{insight_dbkey_meta}
\alias{insight_dbkey_meta}
\title{Retrieve DBHYDRO Insights DBKEY metadata for multiple DBKEYs (safe)}
\usage{
insight_dbkey_meta(dbkeys, timeout_sec = 60, pause_sec = 0)
}
\arguments{
\item{dbkeys}{A character or numeric vector of DBKEY identifiers. Each DBKEY
is requested individually (one HTTP request per DBKEY).}

\item{timeout_sec}{Numeric scalar. Request timeout in seconds passed to
\code{httr::timeout()}. Default is 60.}

\item{pause_sec}{Numeric scalar. Optional delay (in seconds) between requests
to reduce the chance of rate-limiting. Default is 0 (no pause).}
}
\value{
A \code{data.frame} with one row per requested DBKEY. For successful
requests, columns include metadata fields returned by the API (e.g.,
\code{station}, \code{dataType}, \code{frequency}, \code{startDate},
\code{endDate}, coordinates, etc.). The output always contains:
\describe{
\item{\code{dbkey_requested}}{DBKEY value submitted in the request.}
\item{\code{request_ok}}{Logical flag indicating if that request succeeded.}
\item{\code{error_message}}{Character error message when \code{request_ok = FALSE}; otherwise \code{NA}.}
}
}
\description{
Queries the SFWMD DBHYDRO Insights API \code{dbkeyInfo} endpoint for one or
more DBKEYs and returns a combined data frame with one row per requested DBKEY.
}
\details{
Unlike a "fail-fast" implementation, this function is \emph{safe} for batch pulls:
it will continue processing even if one or more DBKEY requests fail. For each
DBKEY, the returned row includes \code{request_ok} and \code{error_message}
indicating whether that specific request succeeded.

For successful requests, the JSON response is flattened and any \code{NULL}
elements are converted to \code{NA} prior to coercion into a 1-row data frame.
The output always includes \code{dbkey_requested} to preserve the original
input DBKEY, even if the API response is missing or malformed.

This function performs one request per DBKEY and then row-binds the results.
Because API responses can vary slightly across DBKEYs, it aligns columns across
all returned rows, filling missing columns with \code{NA} prior to binding.
}
\examples{
\dontrun{
# Successful and failing DBKEYs can be mixed; failures are captured in output
meta <- insight_dbkey_meta(c("91510", "BADKEY", "91512"), pause_sec = 0.1)

# Inspect results
head(meta)

# See which DBKEYs failed
subset(meta, !request_ok)
}

}
\seealso{
\code{\link[httr:GET]{httr::GET()}}, \code{\link[httr:timeout]{httr::timeout()}},
\code{\link[httr:stop_for_status]{httr::stop_for_status()}}, \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}}
}
