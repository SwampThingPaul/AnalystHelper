% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DBHydro_Insight_hydro_meta.R
\name{insight_station_datatype_meta}
\alias{insight_station_datatype_meta}
\title{Retrieve Timeseries Metadata by Station/Site and Parameter from DBHydro Insights}
\usage{
insight_station_datatype_meta(
  station,
  parameter,
  startdate = NULL,
  enddate = NULL,
  frequency = "ALL",
  statistic = "ALL",
  status = "ALL",
  location_type = c("STATION", "SITE"),
  timeout_sec = 60,
  pause_sec = 0
)
}
\arguments{
\item{station}{\code{character(1)}. The station or site name to query
(e.g., \code{"S333_H"}, \code{"S333"}). For \code{location_type = "STATION"},
the SFWMD Insights API typically expects the \verb{_H} suffix for
hydrologic stations (e.g., \code{"S333_H"}).}

\item{parameter}{\code{character(1)}. The parameter code to filter by
(e.g., \code{"STG"} for stage, \code{"FLOW"} for flow,
\code{"RAIN"} for rainfall, \code{"PHOSPHORUS"} for total phosphorus).}

\item{startdate}{\code{character(1)} or \code{NULL}. Start date of the query
window in \code{"YYYY-MM-DD"} format. Defaults to 30 days before the
current system date if \code{NULL}.}

\item{enddate}{\code{character(1)} or \code{NULL}. End date of the query
window in \code{"YYYY-MM-DD"} format. Defaults to the current system date
if \code{NULL}.}

\item{frequency}{\code{character(1)}. Frequency filter for the timeseries.
Common values include \code{"DA"} (daily), \code{"BP"} (breakpoint/
instantaneous), and \code{"ALL"} (no filter). Defaults to \code{"ALL"}.}

\item{statistic}{\code{character(1)}. Statistic filter for the timeseries.
Common values include \code{"MEAN"}, \code{"INST"} (instantaneous), and
\code{"ALL"} (no filter). Defaults to \code{"ALL"}.}

\item{status}{\code{character(1)}. Status filter for the timeseries.
Typically \code{"ALL"}, \code{"Active"}, or \code{"Inactive"}.
Defaults to \code{"ALL"}.}

\item{location_type}{\code{character(1)}. The type of location identifier
supplied in \code{station}. Must be one of \code{"STATION"} (default,
for hydrologic structures and gages) or \code{"SITE"} (for water quality
monitoring sites). Validated via \code{\link[base:match.arg]{base::match.arg()}}.}

\item{timeout_sec}{\code{numeric(1)}. Maximum number of seconds to wait for
an API response before timing out. Defaults to \code{60}.}

\item{pause_sec}{\code{numeric(1)}. Number of seconds to pause before
issuing the API request. Useful for rate-limiting when calling this
function in a loop. Defaults to \code{0} (no pause).}
}
\value{
A \code{data.frame} with one row per matching timeseries, containing
columns returned by the Insights API (which may include \code{timeseriesId},
\code{dbkey}, \code{station}, \code{parameter}, \code{frequency},
\code{statistic}, \code{datum}, \code{status}, among others), plus three
appended columns:
\describe{
\item{\code{station_requested}}{The value passed to \code{station}.}
\item{\code{parameter_requested}}{The value passed to \code{parameter}.}
\item{\code{location_type_used}}{The resolved value of \code{location_type}.}
}
If no timeseries are found or the response cannot be coerced to a
\code{data.frame}, the raw parsed response is returned invisibly and a
diagnostic message is printed listing the top-level names of the API
response.
}
\description{
Queries the SFWMD DBHydro Insights API (\verb{/v1/insights-data/cont/ts}) to
retrieve timeseries metadata — including Timeseries IDs (DBKEYs), frequency,
statistic, datum, and status — for a given station or site and parameter
combination. This function mirrors the filter logic used by the Insights web
UI and is the recommended approach for resolving DBKEYs programmatically
in place of the legacy DBHYDRO Browser \code{show_dbkeys_matched} workflow.
}
\details{
This function POSTs a JSON query to the SFWMD DBHydro Insights continuous
timeseries endpoint (\verb{/v1/insights-data/cont/ts}), replicating the
exact payload structure used by the Insights web UI. The \code{locations}
field in the payload is constructed as:
\preformatted{
  [{"name": "<station>", "type": "<location_type>"}]
}

All filter fields (\code{category}, \code{dbkeys}, \code{recorder},
\code{stationIDs}, \code{usgsIDs}) that are not directly exposed as
arguments default to \code{"ALL"} (no filtering), consistent with the
Insights UI default behavior.

The \code{reporttype} is fixed to \code{"timeseries"} and
\code{reportformat} to \code{"table"}, which returns metadata for each
matching timeseries rather than the underlying data values. To retrieve
actual data, pass the returned \code{timeseriesId} / \code{dbkey} values
to \code{\link[=insight_dbkey_meta]{insight_dbkey_meta()}} or the relevant fetch functions.
}
\note{
\itemize{
\item The SFWMD Insights API is not publicly documented. The endpoint and
payload structure were derived by inspecting browser network traffic
against \url{https://insightsdata.sfwmd.gov} and are subject to change
without notice.
\item For hydrologic stations, the API typically expects the \verb{_H}
suffix (e.g., \code{"S333_H"} rather than \code{"S333"}). For water
quality sites, use \code{location_type = "SITE"} and the bare station
name.
\item This function requires the \pkg{httr} and \pkg{jsonlite} packages.
}
}
\examples{
\dontrun{
# All stage timeseries at S333 (hydrologic station, all frequencies)
meta <- insight_station_datatype_meta(
  station   = "S333_H",
  parameter = "STG"
)

# Daily mean stage only, specific date window
meta_da <- insight_station_datatype_meta(
  station    = "S333_H",
  parameter  = "STG",
  startdate  = "2020-01-01",
  enddate    = "2022-12-31",
  frequency  = "DA",
  statistic  = "MEAN"
)

# Water quality site — total phosphorus
meta_wq <- insight_station_datatype_meta(
  station       = "S333",
  parameter     = "PHOSPHORUS",
  location_type = "SITE"
)
}

}
