% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DBHydro_Insight_hydro_meta.R
\name{insight_station_meta}
\alias{insight_station_meta}
\title{Retrieve Station Metadata from the DBHydro Insights API}
\usage{
insight_station_meta(
  stations,
  timeout_sec = 60,
  pause_sec = 0,
  base_url = "https://insightsdata.api.sfwmd.gov/v1/insights-data",
  station_path = c("stationInfo", "siteInfo", "stationDbkeys", "siteDbkeys"),
  explode_timeseries = TRUE
)
}
\arguments{
\item{stations}{\code{character} vector. One or more station or site
identifiers to query (e.g., \code{c("S333_H", "S11A_H")}). Values are
coerced to character, unlisted, and stripped of \code{NA} or empty strings
before querying.}

\item{timeout_sec}{\code{numeric(1)}. Maximum number of seconds to wait for
an API response before timing out. Defaults to \code{60}.}

\item{pause_sec}{\code{numeric(1)}. Number of seconds to pause before each
individual API request. Useful for rate-limiting when querying many
stations in a loop. Defaults to \code{0} (no pause).}

\item{base_url}{\code{character(1)}. Base URL for the SFWMD DBHydro Insights
API. Defaults to
\code{"https://insightsdata.api.sfwmd.gov/v1/insights-data"}. Change only
if the API host or version path changes.}

\item{station_path}{\code{character} vector. One or more candidate endpoint
path segments to attempt for each station, tried in order until one
succeeds. The full request URL is constructed as
\verb{<base_url>/<station_path>/<station>}. Defaults to
\code{c("stationInfo", "siteInfo", "stationDbkeys", "siteDbkeys")}.
Update this argument if the correct Insights endpoint becomes known.}

\item{explode_timeseries}{\code{logical(1)}. If \code{TRUE} (default), any
nested list-column in the API response whose name matches a known
timeseries/DBKEY candidate (\code{"timeseries"}, \code{"timeSeries"},
\code{"timeseriesList"}, \code{"dbkeys"}, \code{"dbkeyList"},
\code{"data"}) is expanded into one row per element, with station and
endpoint metadata carried through to each row. If \code{FALSE}, the raw
(possibly nested) response is returned as-is.}
}
\value{
A \code{data.frame} combining results across all requested stations,
with one row per station (or one row per timeseries/DBKEY if
\code{explode_timeseries = TRUE} and a nested list-column is found).
Columns vary depending on the API response but always include:
\describe{
\item{\code{station_requested}}{The station identifier passed by the
caller.}
\item{\code{endpoint_used}}{The \code{station_path} segment that
returned a successful response.}
\item{\code{url_used}}{The full URL used for the successful request.}
\item{\code{request_ok}}{\code{logical}. \code{TRUE} if the request
succeeded; \code{FALSE} if all endpoint attempts failed.}
\item{\code{error_message}}{\code{character}. \code{NA} on success;
a diagnostic message if all attempts failed.}
}
When results across stations have differing column sets, missing columns
are filled with \code{NA} before binding rows. Row names are reset to
\code{NULL}.
}
\description{
Queries the SFWMD DBHydro Insights API to retrieve station or site metadata
by iterating over one or more station identifiers. For each station, the
function attempts a sequence of candidate GET endpoints (e.g.,
\code{stationInfo}, \code{siteInfo}) until a successful response is returned.
Optionally, nested timeseries or DBKEY list-columns in the response are
"exploded" into one row per timeseries/DBKEY for downstream use with
\code{\link[=insight_dbkey_meta]{insight_dbkey_meta()}} or \code{\link[=insight_station_datatype_meta]{insight_station_datatype_meta()}}.
}
\details{
For each station in \code{stations}, \code{insight_station_meta()} issues
sequential GET requests across the \code{station_path} candidates, stopping
at the first successful (non-error, non-404) response. This "try until
success" strategy accommodates the fact that the SFWMD Insights API is not
publicly documented at the endpoint level, and the correct path may vary
by station type (hydrologic vs. water quality).

Two internal helpers support robust parsing:
\itemize{
\item \code{null_to_na()} — converts \code{NULL} elements within a parsed
JSON list to \code{NA}, preventing \code{as.data.frame()} failures.
\item \code{as_df_safe()} — wraps \code{as.data.frame()} in
\code{\link[base:conditions]{base::tryCatch()}}, falling back to a single list-column
\code{data.frame} if coercion fails.
}

If \code{explode_timeseries = TRUE} and a recognized nested list-column is
found, only the \strong{first} matching candidate column is expanded. The
expanded rows inherit \code{station_requested}, \code{endpoint_used},
\code{url_used}, \code{request_ok}, and \code{error_message} from the
parent response.

If \strong{all} \code{station_path} attempts fail for a given station (due to
404, timeout, or parse errors), a single diagnostic row is returned for
that station with \code{request_ok = FALSE} and a descriptive
\code{error_message}, rather than stopping the entire batch.
}
\note{
\itemize{
\item The SFWMD Insights API is not publicly documented. Endpoint paths
and response schemas were derived from browser network inspection of
\url{https://insightsdata.sfwmd.gov} and are subject to change without
notice. If all default \code{station_path} attempts return 404, update
\code{station_path} to the correct segment observed in browser
DevTools.
\item This function uses GET requests and is suited for metadata retrieval.
For timeseries data retrieval by station and parameter, see
\code{\link[=insight_station_datatype_meta]{insight_station_datatype_meta()}}, which uses a POST-based
query engine.
\item This function requires the \pkg{httr} and \pkg{jsonlite} packages.
}
}
\examples{
\dontrun{
# Single station — try all default endpoint paths
meta <- insight_station_meta("S333_H")

# Multiple stations with a polite pause between requests
meta <- insight_station_meta(
  stations  = c("S333_H", "S11A_H", "S12A_H"),
  pause_sec = 0.2
)

# Pin to a known working endpoint (skip trial-and-error)
meta <- insight_station_meta(
  stations     = "S333_H",
  station_path = "stationInfo"
)

# Return raw nested response without exploding timeseries list-columns
meta_raw <- insight_station_meta(
  stations           = "S333_H",
  explode_timeseries = FALSE
)

# Check which requests succeeded
meta[, c("station_requested", "endpoint_used", "request_ok", "error_message")]
}

}
