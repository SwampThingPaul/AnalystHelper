% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DBHydro_Insight.R
\name{insight_wq_query}
\alias{insight_wq_query}
\title{Build a DBHydro Insights water-quality (chem) query payload}
\usage{
insight_wq_query(
  locations = "ALL",
  location_type = "STATION",
  parameters = "ALL",
  methods = "ALL",
  projects = "ALL",
  matrices = "ALL",
  paramGroups = "ALL",
  testNumbers = NULL,
  ...
)
}
\arguments{
\item{locations}{Character vector of location identifiers (e.g., station codes).
Use \code{"ALL"} to request all locations.}

\item{location_type}{Character scalar giving the location identifier type used when
\code{locations} is not \code{"ALL"}. Common values include \code{"STATION"} (default),
and other API-supported types.}

\item{parameters}{Character vector of parameter identifiers, or \code{"ALL"}.}

\item{methods}{Character vector of method identifiers, or \code{"ALL"}.}

\item{projects}{Character vector of project identifiers, or \code{"ALL"}.}

\item{matrices}{Character vector of matrix identifiers, or \code{"ALL"}.}

\item{paramGroups}{Character vector of parameter-group identifiers, or \code{"ALL"}.}

\item{testNumbers}{Optional numeric vector (or list) of test numbers to filter by.
If \code{NULL} (default), the field is omitted from the query payload.}

\item{...}{Additional named query elements to include in the payload.}
}
\value{
A nested \code{list} representing the query payload. This object is suitable for JSON
encoding and inclusion in a request body such as \code{list(query = insight_wq_query(...))}.
}
\description{
Constructs a nested list formatted for use as the \code{query} object in
DBHydro Insights chemistry/water-quality report requests (e.g.,
\verb{/v1/insights-data/chem/report/data}). The output is intended to be
JSON-encoded and sent as a request body such as \verb{list(query = <result>)}.
}
\details{
The function standardizes common filter fields (\code{parameters}, \code{methods},
\code{projects}, \code{matrices}, \code{paramGroups}) into lists. Locations are
formatted as a list of \verb{list(name = <...>, type = <...>)} objects.

If \code{locations} is exactly \code{"ALL"}, then the location type is forced to
\code{"ALL"} (regardless of \code{location_type}).

Additional filters supported by the API (e.g., \code{testNumbers}, \code{sampleTypes},
\code{paramGroups}, etc.) can be provided via \code{...} and will be included in the
returned list as-is (after the function adds its standard fields).
}
\examples{
# Single station, include an additional filter (testNumbers)
# single test number
q1 <- insight_wq_query(locations = "S333", testNumbers = 25)

# Multiple stations
q2 <- insight_wq_query(
  locations = c("S333", "G722"),
  testNumbers = c(25)
)

# All locations (location type forced to ALL)
q3 <- insight_wq_query(locations = "ALL")

}
